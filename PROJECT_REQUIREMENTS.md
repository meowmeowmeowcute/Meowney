# 本地離線 PWA 記帳工具：專案需求規格

> 文件狀態：第一版需求基準  
> 需求來源：使用者已確認的五輪需求訪談  
> 目標平台：手機優先的可安裝 PWA  
> 資料原則：單人、本機保存、離線優先、無後端

---

## 0. Codex 執行本文件時的最高優先規則

1. 本專案採「明確需求制」。只有本文件或使用者當前指令明確要求的功能，才屬於專案需求。
2. 不得因業界慣例、最佳實踐、類似應用通常具有、提升完整性或未來可能需要，而自行加入使用者可見功能。
3. 發現可能值得加入但未被要求的功能時，只能在工作結果中提出建議，不得直接實作。
4. 遇到本文件未定義、彼此衝突或足以改變操作方式的事項，必須先詢問使用者，不得自行猜測。
5. 修正功能時採最小修改原則；不得順便重構、改版或修改與當前階段無關的模組。
6. 不得以技術方便為理由改變已確認的互動、資料規則或功能範圍。
7. 每一階段只實作該階段列出的內容。尚未進入的階段只能保留必要介面，不得預先完成。
8. 每階段完成時，必須逐項回報驗收結果、已知限制、測試方式及變更檔案。
9. 使用者的新明確指令可修改本文件；若新指令與本文件衝突，以最新的明確指令為準，並同步更新本文件。

### 絕對停止門檻

> **第一階段 UI Prototype 完成後，Codex 必須停止並等待使用者明確確認。**

- 使用者未明確表示「第一階段通過」、「可以進入第二階段」或同等意思前，不得進入第二階段。
- 不得建立 IndexedDB schema、正式持久化資料層、資料 migration、Service Worker、離線快取、正式匯入匯出或任何後續階段功能。
- 即使 Prototype 的所有自動測試都通過，也不能視為使用者已確認。
- 使用者只提出 UI 修改時，仍停留在第一階段；修改、重新驗收後再次停止等待。
- 「先順便把底層做好」、「只是預留資料庫」或「為後續方便」都不構成越過此門檻的理由。

---

## 1. 專案目標

建立一套供單一使用者日常使用的本地離線記帳工具。應用以靜態網站形式發布，可安裝到手機主畫面；程式第一次成功載入後，在完全沒有網路時仍可進行所有日常資料操作。

核心目標：

- 快速記錄支出、收入及帳戶間轉帳。
- 主頁直接查看各帳戶餘額與依日期分組的歷史交易。
- 以少量可自行修改的母類別為起點，子類別由使用者依實際需要建立。
- 查詢指定母類別或子類別在指定時間範圍內的金額。
- 所有財務資料只保存在目前裝置，不依賴帳號、雲端資料庫或後端服務。
- 透過 JSON 與 CSV 進行可攜式備份、還原與資料交換。

本專案不是完整個人財務平台，也不追求功能數量；快速、清楚、可離線及符合已確認操作方式優先。

---

## 2. 技術與運行邊界

### 2.1 必要架構

- 手機優先的 PWA，主要使用情境為 Android 手機直向畫面。
- 可部署於 GitHub Pages、Cloudflare Pages 或同等靜態 HTTPS 空間。
- 不需要自行架設 Backend、API Server 或資料庫伺服器。
- 正式資料使用瀏覽器本機結構化儲存（IndexedDB）。
- Service Worker 僅用於應用程式資源快取、離線啟動與版本更新，不得把財務資料傳到網路。
- 應用程式不得要求連線才能新增、查詢、修改或刪除本機資料。

### 2.2 隱私與資料位置

- 帳戶、類別、交易及設定預設只存在目前瀏覽器／已安裝 PWA 的本機儲存空間。
- 不得加入遙測、廣告、追蹤、雲端同步或遠端財務資料傳輸。
- 不得暗示卸載 PWA、清除瀏覽器網站資料或重設手機後仍能自動復原；備份檔才是跨清除／跨裝置復原手段。

### 2.3 第一版固定呈現

- 語言：繁體中文。
- 貨幣：固定 `NT$`，不提供多幣別或匯率。
- 外觀：只做深色模式，不提供主題切換。
- 手機版為主要驗收對象；桌面版只需可正常操作，不得為桌面版改變手機互動。

---

## 3. 第一版明確功能範圍

第一版只包含：

1. 主頁／交易紀錄。
2. 快速新增支出、收入與轉帳。
3. 編輯及刪除既有交易。
4. 帳戶新增、編輯、刪除及個別餘額計算。
5. 母類別與子類別新增、編輯、移動及刪除。
6. 獨立查詢頁與文字／數字統計結果。
7. JSON 完整備份與還原。
8. CSV 匯出與匯入。
9. 完整離線運作、可安裝 PWA 與自動更新。
10. 設定頁中已明列的管理入口。
11. 支出可選擇建立連動報銷收入，並可標記為預計請款。

底部固定導覽列至少包含：

- `紀錄`：主頁及交易紀錄。
- `查詢`：篩選與統計。
- `設定`：帳戶、類別與資料管理。

主頁不放複雜臨時篩選；所有篩選集中在獨立查詢頁。

---

## 4. 主頁與交易紀錄

### 4.1 頂部帳戶區

- 顯示目前存在的每一個帳戶名稱與個別目前餘額。
- 不顯示所有帳戶合計的總資產／總餘額。
- 沒有帳戶時顯示清楚的空狀態與前往帳戶管理的入口，但不得自動建立帳戶。

### 4.2 交易列表

- 主頁本身就是交易紀錄頁；使用者直接向下捲動查看歷史紀錄。
- 最新日期與最新交易在上方，較舊內容在下方。
- 交易依日期分組。
- 每個日期標題：日期靠左，該日金錢淨增減靠右。
- 日期淨增減為該日收入減支出；帳戶間轉帳對整體淨增減為 `0`。
- 每筆一般交易不重複顯示日期，因為已由日期分組。
- 每筆支出直接顯示金額、子類別、帳戶與時間。
- 每筆收入直接顯示金額、帳戶與時間；收入不需要子類別。
- 每筆交易如有備註，交易紀錄必須完整顯示備註文字，不得以省略號截斷。
- 已標記的支出於交易列以清楚的「預計請款」小標籤顯示；已建立報銷的原支出不得保留此標籤。
- 「其他」直接支出、一般收入與報銷如有備註，備註文字作為交易列的主要顯示；報銷有備註時，於備註後以灰色小字標示「報銷」；無備註時才顯示「其他」、「收入」或「報銷」。
- 日期標題以實際日期為主要文字；今天或昨天僅以灰色小字附在日期後方。
- 支出以紅色及 `-NT$ xxx` 顯示。
- 收入以綠色及 `+NT$ xxx` 顯示。
- 轉帳以清楚的 `來源帳戶 → 目的帳戶` 表示，不套用收入／支出顏色語意，也不列入收入或支出。
- 點擊既有交易後，從底部滑出與新增介面一致風格的編輯面板，不跳轉到新頁。

### 4.3 浮動「記帳」按鈕

- 位於主頁中下方，浮在交易紀錄上方。
- 外型是橫向圓角長條，文字為 `記帳`。
- 不占用交易列表的固定版面高度，不應遮住最後一筆交易或底部導覽。
- 點擊後由底部滑出快速記帳面板，主頁仍留在背後。

---

## 5. 快速記帳互動

### 5.1 共通行為

- 面板由底部滑出，不進入獨立頁面。
- 預設交易類型為 `支出`；只有需要時才切換為 `收入` 或 `轉帳`。
- 日期與時間預設為開啟面板當下的日期與時間，使用者未修改即可直接使用。
- 一般記帳不要求先選日期。
- 面板需包含：
  - 支出／收入／轉帳切換
  - 金額與固定 `NT$` 顯示
  - 螢幕內數字鍵盤
  - 支出時顯示的母類別切換區
  - 支出時顯示的所選母類別子類別選擇區
  - 帳戶選擇
  - 備註
  - 日期與時間修改入口
  - `確定` 操作
- 快速記帳主要互動與視覺排列，以需求訪談所附參考截圖為 Prototype 基準；若截圖與本文件文字衝突，以本文件文字為準。
- 必填資料不完整時不得儲存，並在原面板內指出缺少欄位，不得清空已輸入內容。
- 儲存成功後面板關閉，交易立即出現在正確日期分組，帳戶餘額與日期淨增減同步更新。

### 5.2 支出與收入

每筆支出必須具有大於 `0` 的金額、一個目前存在的帳戶、一個子類別（不能只選母類別）、日期、時間與可選填備註；但預設母類別「其他」可直接選取，不需要子類別。

每筆收入必須具有大於 `0` 的金額、一個目前存在的帳戶、日期、時間與可選填備註；收入不需要母類別或子類別。

支出使指定帳戶餘額減少；收入使指定帳戶餘額增加。

支出可選擇「預計請款」狀態；此狀態只適用於支出，供查詢尚未請款的項目使用。

支出可選擇「新增報銷」。確認後必須以同一原子操作新增一筆同帳戶、同日期時間的連動收入；報銷金額獨立設定，預設帶入支出金額但可填部分報銷，且必須大於 `0`。報銷備註預設帶入原支出備註，但可另行改寫。報銷如有備註，以備註作為交易列主要顯示，並於後方以灰色小字標示「報銷」；無備註時顯示「報銷」。報銷收入不需要類別，並與原支出雙向連動；報銷金額可與支出金額不同，因此帳戶淨額依兩筆實際金額計算。建立或重新建立報銷時，原支出的「預計請款」狀態必須在同一原子操作中自動取消。

### 5.3 轉帳

- 必須選擇不同的來源帳戶與目的帳戶。
- 來源帳戶減少相同金額，目的帳戶增加相同金額。
- 轉帳不算收入、不算支出、不需要任何母類別或子類別。
- 轉帳不得改變所有帳戶合計金額。
- 轉帳的建立、編輯與刪除必須是單一原子操作，不得只更新其中一個帳戶。
- 底層可採一筆含來源／目的的記錄或內部連結記錄，但使用者介面必須把它視為一筆完整轉帳。

### 5.4 編輯與刪除交易

支出可修改金額、子類別、帳戶、日期、時間、備註與預計請款狀態；收入可修改金額、帳戶、日期、時間與備註，且不需要類別。

修改含報銷的原支出時，連動報銷的帳戶、日期與時間必須同步更新，但報銷金額保持獨立、不隨支出金額變動；報銷交易本身可修改其報銷金額與備註。刪除原支出時必須一併刪除連動報銷；刪除報銷時則保留原支出並解除連動。

轉帳可修改金額、來源帳戶、目的帳戶、日期、時間與備註。

所有交易都可刪除。修改或刪除後，所有受影響帳戶餘額、日期分組及統計必須立即重新正確計算。刪除必須有明確確認，避免誤觸；不得加入設定頁的「清除所有資料」。

---

## 6. 帳戶規則

### 6.1 建立與編輯

- 第一次使用時完全沒有預設帳戶，由使用者自行建立。
- 建立帳戶只需要：
  - 帳戶名稱
  - 初始餘額
- 所有帳戶都可以編輯名稱與初始餘額。
- 不加入帳戶圖示、顏色或備註。

### 6.2 餘額計算

帳戶目前餘額必須由以下資料一致計算：

`初始餘額 + 收入 - 支出 + 轉入 - 轉出`

修改帳戶初始餘額或任何歷史交易後，顯示餘額必須反映最新結果。不得把可與交易推導結果不一致的快取餘額當成唯一真實資料。

### 6.3 刪除帳戶

- 即使帳戶已有歷史交易，仍允許刪除帳戶。
- 刪除帳戶不得連帶刪除或破壞任何歷史交易。
- 歷史交易在帳戶刪除後仍須顯示當時可辨識的帳戶名稱，不得只顯示失效 ID 或空白。
- 已刪除帳戶不可再用於新交易。

---

## 7. 類別規則

### 7.1 初始狀態與管理

- 系統可提供少量母類別作為初始設定，例如購物、吃喝、交通、娛樂、生活。
- 不得預先建立大量子類別；子類別由使用者依需要建立。
- 母類別與子類別都可新增、重新命名及刪除。
- 子類別必須隸屬一個母類別，並可移動至其他母類別。
- 所有支出都必須選擇子類別，不能只記到母類別；唯一例外是預設母類別「其他」，可直接記錄支出。收入不需要母類別或子類別。

### 7.2 刪除與歷史資料

- 子類別即使已有歷史交易仍可刪除。
- 刪除子類別後，過去交易仍須顯示原本的子類別名稱。
- 母類別若仍有子類別，不得直接刪除；必須先要求使用者將所有子類別移到其他母類別。
- 歷史交易不得因帳戶、母類別或子類別日後被刪除或改動，而失去原本可辨識的顯示資訊。
- 資料設計必須保存足以顯示歷史名稱的快照或同等機制，不能只依賴仍存在的關聯 ID。

---

## 8. 查詢與統計

### 8.1 查詢入口與條件

查詢位於底部導覽的獨立 `查詢` 頁。可組合使用以下條件：

- 日期條件
- 母類別
- 子類別
- 帳戶
- 支出／收入
- 請款狀態（全部或尚未請款）
- 備註關鍵字
- 金額範圍

日期需提供：

- 今天
- 本月
- 特定日期
- 特定月份

金額範圍以正數金額比較，收入／支出方向由類型條件決定。備註搜尋至少支援包含式關鍵字比對。

### 8.2 母類別結果

選定母類別與時間條件後，顯示：

- 該母類別符合條件的總支出。
- 該母類別下各子類別分別的支出金額。

第一版母類別結果不需要自動列出所有交易，也不需要圖表。

### 8.3 子類別結果

選定子類別與時間條件後，顯示：

- 符合條件的總花費。
- 符合條件的交易筆數。
- 所有符合條件的交易紀錄。

### 8.4 收入、支出與轉帳統計

- 同一次查詢可同時顯示收入與支出，但必須分別列出兩個總額。
- 不得只顯示收入減支出的淨額來取代個別總額。
- 轉帳不計入收入或支出總額，也不計入母／子類別花費。
- 第一版不做圓餅圖、長條圖、折線圖或其他圖表。

### 8.5 查詢正確性

- 所有篩選組合採交集條件。
- 邊界日期包含起訖當日。
- 修改或刪除交易後，重新查詢必須立即得到更新結果。
- 已刪除帳戶或類別的歷史交易仍可依其保留的歷史資訊正確顯示；不得因關聯項目已刪除而漏掉交易。

---

## 9. 備份、還原、匯出與匯入

### 9.1 JSON 完整備份

- 可匯出包含所有帳戶、類別、交易、轉帳、必要設定、資料格式版本與匯出時間的完整 JSON。
- 匯出檔需可由本應用程式重新匯入，並足以完整重建資料。
- 不把 PWA 快取或純介面狀態當成備份資料。

### 9.2 JSON 還原

- JSON 匯入採「完全覆蓋目前資料」，不提供合併模式。
- 覆蓋前必須明確告知目前資料將被取代，並要求使用者確認。
- 寫入前必須完整驗證檔案版本、結構、必要欄位、ID／關聯與資料型別。
- 驗證失敗不得修改任何現有資料。
- 還原必須具備交易性：全部成功或完全不變，不得留下半完成狀態。

### 9.3 CSV 匯出

- 可匯出供試算表閱讀的交易資料。
- 至少包含：交易識別、類型、金額、帳戶、來源帳戶、目的帳戶、母類別、子類別、日期、時間、備註。
- 欄名、日期格式、編碼與跳脫規則必須固定並記錄在專案文件中。
- CSV 是資料交換格式，不取代 JSON 完整備份。
- 「其他」直接支出保留母類別欄位，子類別 ID 與名稱欄位留白。
- CSV 以「報銷支出交易ID」欄位保留報銷收入與原支出的關聯；以「預計請款」欄位的「是」保留尚未請款支出狀態。報銷收入必須與原支出同帳戶、同日期時間，但金額可獨立且允許部分報銷；原支出已連動報銷時不得保留預計請款狀態，關聯或狀態錯誤時整批不寫入。

### 9.4 CSV 匯入

- 第一版必須支援 CSV 匯入。
- 至少必須能重新匯入本應用程式自己匯出的標準 CSV 格式。
- 匯入前提供欄位與錯誤預覽；格式或必要欄位錯誤時不得部分寫入。
- CSV 採已確認的原子合併規則：既有交易 ID 且內容完全相同時跳過；既有 ID 但內容不同，或 CSV 內有重複 ID 時視為衝突，整批不寫入。
- 缺少帳戶時依名稱建立，初始餘額為 `0`；缺少類別時依母類別與子類別名稱建立必要結構。
- 任何格式、資料型別、轉帳關聯或母子類別關聯錯誤時，整批不寫入。
- 寫入前必須預覽新增、跳過、建立帳戶、建立類別及衝突筆數；確認後以單一資料庫交易完成，任一失敗不得部分成功。
- CSV 中「其他」直接支出必須填寫母類別「其他」，且子類別 ID 與名稱皆留白；其餘支出仍須具備完整母子類別關聯。
- 「預計請款」只可填在一般支出；收入、轉帳與報銷原支出同時保留預計請款時，整批不寫入。

### 9.5 明確不做

- 不做自動雲端備份。
- 不做備份提醒或通知。
- 不做設定頁「清除所有資料」。

---

## 10. PWA 離線與更新

### 10.1 完整離線

第一次在線載入並完成必要資源快取後，完全離線時必須仍可：

- 開啟應用程式。
- 查看帳戶餘額與交易紀錄。
- 新增、修改、刪除支出與收入。
- 新增、修改、刪除轉帳。
- 查詢及計算統計。
- 管理帳戶、母類別與子類別。
- 執行本機 JSON／CSV 匯入匯出（受瀏覽器檔案選擇與下載能力限制）。

應用程式殼層、必要圖示、字型與執行所需靜態資源都必須可離線取得。不得在執行期依賴未快取的 CDN 資源。

### 10.2 安裝

- 提供有效 Web App Manifest、應用程式名稱、圖示、啟動畫面所需設定與獨立顯示模式。
- 經 HTTPS 部署後應符合目標瀏覽器的可安裝條件。
- 從手機主畫面開啟時，核心操作不得依賴瀏覽器網址列。

### 10.3 自動更新

- 發現新版時自動下載／準備更新，於下次開啟套用。
- 不要求使用者按「立即更新」。
- 不得在使用者正在輸入或寫入資料時強制重新載入。
- 更新應保留 IndexedDB 中的使用者資料；需要 schema migration 時必須可重複、安全且有失敗處理。
- Service Worker 不得造成舊版程式與新版資料 schema 永久不相容；更新流程必須有版本驗證測試。

---

## 11. 設定頁

設定頁只包含以下功能群組：

1. 帳戶管理。
2. 母類別管理。
3. 子類別管理及移動母類別。
4. JSON 備份／還原。
5. CSV 匯出／匯入。

設定頁不得加入：

- 清除所有資料。
- 深色／淺色切換。
- 貨幣切換。
- 登入、同步、通知、預算或其他未列功能。

---

## 12. 第一版明確禁止的功能

下列功能不只是「暫緩」，而是第一版禁止 Codex 自行新增：

1. 信用卡及信用卡帳單功能。
2. 預算。
3. 圖表。
4. 雲端同步或雲端備份。
5. 登入／註冊。
6. 多使用者。
7. 多幣別與匯率。
8. OCR／發票辨識。
9. AI 自動分類。
10. 定期交易。
11. 通知與備份提醒。
12. 通用標籤（本文件明確指定的「預計請款」狀態除外）。
13. 商家管理。
14. 附件／照片。

需求問卷中的「其他」沒有提供具體內容，因此不得自行推測第 15 個禁止項目，也不得藉此新增任何功能。

此外，不得自行加入：總資產卡片、主頁篩選、預設大量子類別、帳戶圖示／顏色／備註、類別圖示庫、主題選擇、資料分析 Dashboard、後端服務或原生 APK 專屬功能。

第一輪曾提及信用卡消費，但後續已明確決定第一版完全移除信用卡；以後續決定為準。

---

## 13. 資料完整性與共同驗收原則

### 13.1 必須成立的不變條件

- 金額必須大於 `0`，方向由交易類型決定。
- 支出必須有帳戶與子類別；「其他」直接支出是唯一可免子類別的例外。收入必須有帳戶，但不需要類別。
- 轉帳的來源與目的帳戶不可相同。
- 轉帳的兩端金額永遠相等，並以原子方式變更。
- 轉帳永遠不列入收入、支出或類別統計。
- 報銷與原支出的雙向連動、帳戶、日期時間必須一致；兩筆金額各自大於 `0` 且互不連動，新增、同步修改與刪除不得留下單邊交易。原支出一旦連動報銷，預計請款狀態必須為否。
- 刪除帳戶或類別不會刪除歷史交易。
- 所有歷史交易始終具有可辨識的帳戶／類別顯示文字。
- JSON 還原及 CSV 寫入不得在失敗時留下部分資料。

### 13.2 驗收環境

- 至少使用一個手機尺寸直向 viewport 進行互動驗收。
- PWA 階段至少在目標 Android Chromium 瀏覽器驗證安裝與離線啟動。
- 所有重要資料規則需有自動化測試；視覺與手勢互動另以實機／瀏覽器人工驗收。
- 不得只以桌面寬螢幕或單元測試通過宣告完成。

### 13.3 無障礙與基本品質

- 按鈕需有可辨識文字或無障礙名稱。
- 文字、金額與狀態在深色背景上需有足夠對比。
- 鍵盤開啟時，金額、確定操作與目前輸入不得被遮住。
- Bottom Sheet 開啟時應正確管理焦點與返回／關閉行為。
- 錯誤訊息必須可理解，不得只顯示技術例外。

---

## 14. 分階段執行流程與驗收標準

## 階段 1：UI Prototype（純介面與互動）

### 目標

先確認手機上的資訊排列、點擊流程、Bottom Sheet、快速記帳及頁面切換是否符合每日使用習慣。

### 可實作內容

- 深色手機版主頁。
- 頂部多帳戶個別餘額的假資料。
- 依日期分組的交易假資料、日期淨增減與收入／支出顏色。
- 中下方浮動橫向圓角 `記帳` 按鈕。
- 新增交易 Bottom Sheet 的完整可點擊互動。
- 點擊交易後的編輯 Bottom Sheet。
- 支出／收入／轉帳切換、類別、帳戶、備註、日期、時間、數字鍵盤及確認互動。
- `紀錄／查詢／設定` 底部導覽。
- 查詢頁與設定頁的介面 Prototype。
- 可使用固定假資料或重新整理即消失的記憶體狀態模擬新增／編輯／刪除。

### 本階段禁止

- IndexedDB、LocalStorage 或其他正式持久化。
- 正式資料 schema 或 migration。
- Service Worker、PWA 安裝、離線快取。
- 正式 JSON／CSV 讀寫。
- 後端、網路同步或任何第二階段以後功能。

### 驗收標準

- 手機直向畫面可清楚看到個別帳戶餘額，但沒有總資產。
- 交易按日期由新到舊分組，列內只顯示已要求的資訊。
- 浮動 `記帳` 按鈕位置、形狀與遮擋行為正確。
- 點擊 `記帳` 由底部開啟面板；不跳頁。
- 支出為預設類型，日期與時間預設現在。
- 使用者可完整走過支出、收入與轉帳的模擬操作。
- 點擊既有交易可由底部開啟編輯面板。
- 三個底部導覽頁皆可到達，返回與關閉行為一致。
- 使用者能在實際手機尺寸上檢查主要操作。

### 階段完成動作——強制停止

Codex 必須提供 Prototype 的開啟方式、驗收清單、截圖或可檢查畫面及已知差異，然後**停止工作並等待使用者明確確認**。未確認不得開始階段 2。

### 第一階段 UI Prototype 補充確認（使用者明確指示）

- 主頁帳戶卡可點選；點選後，下方交易紀錄僅顯示該帳戶相關交易。再次點選同一帳戶卡時，回到全部帳戶紀錄。此互動在第一階段僅操作記憶體假資料，不得建立持久化或正式篩選資料層。
- 交易紀錄的視覺呈現以使用者提供的參考圖為方向：日期標題與當日金額分開呈現，每筆交易為獨立、易觸碰的深色圓角卡片；既有資訊與操作流程不得改變。
- 母子類別選取的視覺呈現以參考圖為方向：母類別以橫向分頁切換，所屬子類別以方格按鈕呈現；不得新增類別圖示庫、預設大量子類別或任何未列功能。
- 本次工作只可調整第一階段既有 UI Prototype 的視覺與記憶體內互動；完成後必須再次停止，等待使用者確認 UI 最終定稿。

---

## 階段 2：資料模型與本機資料庫

### 前置條件

使用者已明確確認階段 1 可進入下一階段。

### 目標

在不任意改動已確認 UI 的前提下，建立 IndexedDB 資料模型、版本管理、驗證與資料存取層。

### 必要內容

- 帳戶、母類別、子類別、支出／收入、轉帳及設定的 schema。
- 歷史帳戶／類別名稱保存機制。
- 轉帳原子性與關聯完整性。
- schema 版本與可安全重跑的 migration 機制。
- Repository／資料存取介面與自動化測試。
- 不把衍生餘額當成無法重建的唯一資料來源。

### 驗收標準

- 新資料庫建立成功，重新開啟後資料仍存在。
- 帳戶餘額公式與轉帳不變條件測試通過。
- 刪除帳戶／子類別後，測試資料中的歷史交易仍保有名稱。
- 寫入失敗不留下半筆轉帳或損壞關聯。
- UI 沒有未經使用者同意的重新設計。

---

## 階段 3：核心交易功能

### 目標

將已確認的主頁與 Bottom Sheet 接上正式資料，使支出、收入及交易編輯／刪除可實際使用。

### 必要內容

- 新增支出與收入。
- 主頁正式交易列表與日期分組。
- 日期淨增減、個別帳戶餘額。
- 編輯所有已允許欄位。
- 刪除單筆交易與確認流程。
- 空狀態、欄位驗證及錯誤處理。

### 驗收標準

- 新增交易後重新啟動仍存在。
- 支出／收入正確更新指定帳戶、日期標題及列表排序。
- 編輯金額、帳戶、類別、日期或時間後，舊影響被回復、新影響正確套用。
- 刪除後相關餘額與統計基礎資料正確。
- 必填欄位錯誤不會寫入半成品。

---

## 階段 4：帳戶、類別與轉帳管理

### 目標

完成正式帳戶／類別管理與帳戶間轉帳規則。

### 必要內容

- 無預設帳戶的首次使用狀態。
- 帳戶新增、編輯、刪除。
- 少量預設母類別；不預建大量子類別。
- 母／子類別新增、編輯、移動與刪除限制。
- 轉帳新增、編輯、刪除及顯示。
- 刪除關聯項目後的歷史資料顯示。

### 驗收標準

- 帳戶只有名稱與初始餘額欄位，所有帳戶可編輯。
- 刪除有歷史交易的帳戶不會刪除交易。
- 刪除有歷史交易的子類別後，舊交易仍顯示原名稱。
- 有子類別的母類別不能直接刪除，必須先完成移動。
- 轉帳使一端減少、一端增加，合計不變，統計收入與支出皆不變。
- 編輯／刪除轉帳不會產生單邊金額。

---

## 階段 5：查詢與統計

### 目標

完成獨立查詢頁與已確認的數字／清單統計，不加入圖表。

### 必要內容

- 全部八類篩選條件及四種日期選項。
- 母類別總支出與子類別拆分。
- 子類別總花費、筆數及交易清單。
- 收入與支出的分開總額。
- 轉帳排除規則。

### 驗收標準

- 單一條件與多條件交集結果皆正確。
- 今天、本月、特定日期、特定月份的邊界測試正確。
- 母類別金額等於其符合條件的子類別金額合計。
- 子類別總額、筆數與清單互相一致。
- 收入／支出分列，轉帳不污染任何類別或收支總額。
- 頁面中沒有圖表或未要求的 Dashboard。

---

## 階段 6：PWA、完整離線與更新

### 目標

讓已完成的應用可安裝、可完整離線運作，並於下次啟動安全套用新版。

### 必要內容

- Manifest 與必要圖示。
- Service Worker 與應用程式殼層快取。
- 無網路啟動及所有核心操作。
- 新版背景準備、下次開啟套用。
- 更新與資料 schema 相容性測試。

### 驗收標準

- 在線首次載入後切換飛航模式，重新啟動仍可使用。
- 離線可完成 CRUD、轉帳、查詢、帳戶與類別管理。
- 必要資源沒有未處理的 CDN 依賴。
- 可由支援的 Android 瀏覽器安裝並從主畫面啟動。
- 發布新版本後，下次啟動套用更新且原資料完整保留。
- 更新不會在使用者編輯中途強制重載。

---

## 階段 7：備份與資料交換

### 目標

完成 JSON 完整備份／覆蓋還原及 CSV 匯出／匯入。

### 必要內容

- 有版本資訊的完整 JSON 匯出。
- 經驗證與確認後完整覆蓋的 JSON 還原。
- 固定、文件化的 CSV 格式與匯出。
- CSV 匯入預覽與驗證。
- 在使用者確認 CSV 衝突規則後，完成交易性寫入。

### 驗收標準

- JSON 匯出後可在乾淨資料庫完整還原，帳戶餘額、歷史名稱、轉帳及查詢結果一致。
- JSON 無效檔案或取消確認時，既有資料完全不變。
- CSV 可由常見試算表以 UTF-8 正確開啟，中文、逗號、換行與引號不損壞。
- 應用自身匯出的 CSV 可通過自身匯入驗證。
- CSV 寫入失敗不會部分匯入。
- 沒有合併模式的 JSON 還原、雲端備份、通知或清除所有資料功能。

---

## 階段 8：整體穩定化與交付

### 目標

只修正已確認功能的錯誤、一致性、手機操作與文件，不擴充產品範圍。

### 必要內容

- 完整回歸測試。
- 手機 viewport 與目標 Android 瀏覽器實測。
- 離線、更新、備份還原及資料 migration 測試。
- 使用說明、備份警告、部署與復原文件。
- 檢查並移除 Prototype 假資料及除錯入口。

### 驗收標準

- 本文件所有功能與禁止項目逐項核對。
- 所有自動化測試通過，且沒有未說明的失敗或跳過測試。
- 主要手機操作路徑可完成，沒有被鍵盤、Bottom Sheet 或底部導覽遮擋。
- 完全離線時可連續新增、修改、查詢與重新啟動。
- 備份還原演練成功並有清楚文件。
- 沒有自行新增第一版禁止功能。

---

## 15. Codex 每階段交付格式

每個階段完成時，回報至少包含：

1. 本階段實際完成項目。
2. 對照本階段驗收標準的逐項結果。
3. 使用者可重現的啟動與測試方式。
4. 已執行的自動化與人工測試，以及結果。
5. 已知限制、未完成事項或需使用者決定的問題。
6. 實際修改的檔案清單。
7. 是否改動既有 UI；若有，必須指出依據的使用者確認。

第一階段的回報最後必須明確寫出：

> **第一階段 UI Prototype 已完成。目前已停止；在使用者明確確認前，不會建立資料庫或進入任何後續階段。**
