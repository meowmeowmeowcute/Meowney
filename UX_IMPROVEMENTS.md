# Meowney 操作便利性改善紀錄

本文件記錄 2026-09-03 針對手機實際操作流程所做的 UX 檢查。檢查以 390 × 844 直向畫面進行，重點是減少步驟、捲動、重複輸入、頁面切換及誤操作，而非單純改變視覺風格。

## 操作成本基準

改善前，一筆一般支出至少需要：開啟記帳、逐位輸入金額、選母類別、選子類別、選帳戶、向下捲動、確認。即使只有一個帳戶或連續使用相同類別，也要重新選擇。

## 已確認並完成的前五項

### 1. 金額鍵盤與確認操作距離過遠（P1）

- 原因：支出首屏先出現類別、帳戶及兩個低頻報銷區塊；數字鍵盤與確認位於下方。390 × 844 畫面中，確認按鈕原本約位於 Y=1298。
- 改善：數字鍵盤移至金額下方；確認／刪除操作移為 Bottom Sheet 固定底列。
- 效果：開啟後可立即輸入金額，完成操作不需捲到內容底端。

### 2. 帳戶與類別缺少合理預設（P1）

- 原因：每次新增都清空帳戶與類別，首頁已選帳戶也不會帶入。
- 改善：依序採用首頁目前篩選帳戶、該類型上次成功使用的有效帳戶、唯一帳戶；支出另記住上次有效母／子類別，收入及轉帳各自保存適用預設。
- 效果：常見情況少選一至三個欄位；已刪除項目不會被帶入。

### 3. 首次使用缺少直接入口（P1）

- 原因：首頁只有文字要求前往設定，記帳按鈕仍會進入無法儲存的表單。
- 改善：空狀態加入「建立第一個帳戶」；無帳戶時按記帳亦直接開啟帳戶建立表單；初始餘額預設為 0。
- 效果：不再進入沒有帳戶可選的死路，同時保留「不得自動建立帳戶」規則。

### 4. 驗證錯誤未定位缺漏欄位（P1）

- 原因：錯誤只顯示在面板底部，使用者要自行往上尋找帳戶或類別。
- 改善：將第一個缺漏區塊捲至畫面中央、加上錯誤外框，並把焦點移到第一個可操作控制項。
- 效果：錯誤可直接修正，且既有輸入不會清空。

### 5. 手機返回鍵沒有應用內層級（P1）

- 原因：頁面與 Bottom Sheet 未建立瀏覽歷史狀態，Android 返回可能直接離開 PWA。
- 改善：為底部導覽、Bottom Sheet 與刪除確認建立歷史狀態；返回時逐層關閉或回到上一頁。
- 效果：返回行為符合手機預期，降低誤離開。

## 尚未實作，等待後續決定

### P1

- 連續記帳仍會在每筆成功後關閉；是否增加「儲存並新增下一筆」及保留哪些欄位，需先確認產品方向。
- 查詢條件目前有些立即套用、有些必須按「套用條件」，且沒有一鍵清除。
- 母／子類別順序由資料 ID 間接決定，位置不夠穩定；需決定採建立順序或手動排序。
- 合併報銷會預選不同帳戶的全部結果，可能到建立時才提示帳戶不一致。

### P2

- 一般查詢只顯示總額與筆數，只有指定子類別或尚未請款時才顯示交易清單。
- 重點已選母類別仍會清除子類別。
- 少於兩個帳戶時仍可進入無法完成的轉帳流程。
- 關閉有內容的新增／編輯面板會直接捨棄未儲存輸入。
- 編輯既有交易時不能修正交易類型；若開放會牽涉餘額、類別及報銷關聯。
- 合併報銷成功後仍停留在尚未請款結果，查看新收入需切回紀錄頁。
- 設定頁各列皆使用相同的「編輯／刪除」名稱，操作對象辨識度可再提高。

## 已合理，不建議為修改而修改

- 三個底部導覽入口與紀錄頁浮動記帳按鈕層級簡單。
- 支出為預設類型；日期時間自動帶入並預設收合。
- 收入不要求類別；轉帳在資料上視為一筆完整操作。
- 點交易以 Bottom Sheet 編輯，不跳轉新頁。
- 帳戶卡可一鍵篩選，再點一次回到全部。
- 刪除、JSON 完整還原及 CSV 寫入前的確認具有必要性，應保留。
- CSV 先預覽再寫入、報銷欄位自動帶入及儲存後立即更新餘額的方向合理。
